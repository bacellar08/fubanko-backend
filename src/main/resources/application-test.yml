spring:

  datasource:
    url: jdbc:h2:mem:fubanko-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  flyway:
    enabled: true
    url: jdbc:h2:mem:fubanko-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    user: sa
    password:


jwt:
  security:
    secret-key: abcde123
